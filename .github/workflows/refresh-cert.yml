name: 'Refresh Certificate'
on:
  workflow_dispatch:

jobs:
  refresh-certificate:
    runs-on: ubuntu-20.04
    steps:
    - name: 'Install tools'
      run: sudo apt install -y certbot openssl python3-certbot-dns-cloudflare
    - name: 'Run Certbot'
      run: certbot --version
